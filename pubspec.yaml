name: ocean_station_auto
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cached_network_image: ^3.2.0
  crypt: ^4.0.1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0 
  json_serializable: ^6.1.3
  map: ^1.0.0
  mysql1: ^0.19.2
  path_provider: ^2.0.8
  syncfusion_flutter_charts: ^19.3.57
  webview_windows: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^1.3.1

flutter:
  uses-material-design: true

  generate: true

  assets:
    - assets/images/
